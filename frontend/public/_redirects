# Cloudflare Pages _redirects

# Fix canonical redirect: Google crawled /terms-of-service but route is /terms
/terms-of-service /terms 301

# Fix broken resume URL: Google crawled /resume.pdf but actual file is /devkantkumar-resume.pdf
/resume.pdf /devkantkumar-resume.pdf 301

# Security: return 404 for /admin paths — actual panel is at a different location
# This prevents scanners/bots from knowing an admin panel exists
/admin /404 404
/admin/* /404 404

# SPA catch-all: serve index.html for all routes (required for React Router)
/* /index.html 200
